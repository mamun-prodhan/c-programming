#include<stdio.h>

int main(){

    int a, b, c;
    char name;

    scanf("%d %d %d", &a, &b, &c);

    scanf(" %c", &name);

    printf(" a = %d b = %d c = %d name = %c" , a, b, c, name);

    return 0;
}